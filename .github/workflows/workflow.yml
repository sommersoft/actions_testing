name: Schedule testing

on:
  push:
    branches:
      - schedule
  schedule:
    - cron: '*/15 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.repository == 'adafruit/actions_testing'
    steps:
      - name: Echo repo name
        run: echo ${{ github.repository }}
